{
  "name": "backend-marketiv",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:e2e": "playwright test",
    "sync:scopes": "tsx appwrite/sync-scopes.ts"
  },
  "dependencies": {
    "@opendataloader/pdf": "^2.5.0",
    "appwrite": "^26.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "node-appwrite": "^14.1.0",
    "tsx": "^4.19.0",
    "vitest": "^4.1.10"
  }
}
