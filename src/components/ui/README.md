# UI Components

This directory contains reusable, "dumb" UI components that are purely presentational.
Examples: `Button`, `Input`, `Card`, `Modal`, etc.
These components should not contain business logic or data fetching.
