# Feature Components

This directory contains "smart" components that are tied to specific features or business logic.
Examples: `CampaignForm`, `RateCardList`, `CreatorProfile`, etc.
These components may handle state management, data fetching, or complex interactions.
