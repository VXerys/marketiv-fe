# Layout Components

This directory contains structural components used to define the layout of the application.
Examples: `Header`, `Footer`, `Sidebar`, `BottomNavigation`, etc.
These components are typically used in `layout.tsx` or page wrappers.
