# TypeScript Types

This directory contains shared TypeScript interfaces and type definitions.
Examples: `User`, `Campaign`, `Product`, `ApiResponse`, etc.
Defining types here promotes consistency and reusability across the application.
