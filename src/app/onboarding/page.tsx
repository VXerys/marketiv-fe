import { OnboardingPage } from "@/components/features/onboarding";

export const metadata = {
  title: "Lengkapi Profil — Marketiv",
};

export default function Onboarding() {
  return <OnboardingPage />;
}
